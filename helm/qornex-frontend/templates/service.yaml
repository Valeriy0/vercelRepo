apiVersion: v1
kind: Service
metadata:
  name: {{ include "qornex-frontend.fullname" . }}
  labels:
    {{- include "qornex-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qornex-frontend.selectorLabels" . | nindent 4 }}
